export const siteConfig = {
  name: "Al Kabir Lighting",
  baseUrl: process.env.NEXT_PUBLIC_BASE_URL || "https://www.alkabirlighting.com", // Replace with your actual live domain
  // IMPORTANT: Change this to `true` when the site goes live to allow search engines to index it.
  allowIndexing: false,
};
